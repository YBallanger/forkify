name: forkify
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  firebase_auth: ^5.3.4
  firebase_core: ^3.9.0
  flutter:
    sdk: flutter
  flutter_map: ^8.1.1
  flutter_svg: ^2.0.16
  geolocator: ^13.0.2
  go_router: ^14.6.2
  google_sign_in: ^6.2.2
  http: ^1.2.2
  latlong2: ^0.9.1
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  lints: ^5.1.1

flutter:
  uses-material-design: true

  assets:
    - asset/
    - asset/medal/
    - asset/someStats/
